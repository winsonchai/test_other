docker build -t my-node-app:v1 .

docker build --no-cache -t my-node-app:v1 .

docker tag my-node-app:v1 winsonchai2009/test:v1
docker push winsonchai2009/test:v1




# Check if your pods are running successfully
kubectl get pods

# Check if your deployment and services are bound correctly
kubectl get deployments
kubectl get services
kubectl describe pod node-app-deployment

# View the running pods inside the dev-ui space
kubectl get pods -n dev-ui

# View your service mappings
kubectl get svc -n dev-ui

# View the active Ingress route paths
kubectl get ingress -n dev-ui

kubectl get ingress -n scenarioiq


node -e "require('net').connect(3306, 'tpt-md-ssbe01.wdc.com', () => { console.log('✅ NETWORK SOCKS OPEN - DATABASE REACHABLE!'); process.exit(0); }).on('error', (e) => { console.log('❌ NETWORK BLOCKED:', e.message); process.exit(1); })"

kubectl apply -f deployment.yaml

# 3. FORCE KUBERNETES TO PULL THE FRESH IMAGE 🚀
kubectl rollout restart deployment node-app-deployment

close vpn 
$env:ACT_DOCKER_SOCK="\\.\pipe\docker_engine"; act push --secret-file .env.secrets --pull=false -P ubuntu-latest=node:20-alpine
